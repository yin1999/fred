pre-commit:
  parallel: true
  jobs:
    - run: npx prettier --write --cache --ignore-unknown {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  jobs:
    - run: npx tsc --noEmit

    - run: npx eslint --cache

output:
  - summary
  - failure
